# __Команда "git merge"__

1. Объединяет текущую ветку с заданной.
```bash
git merge <branch_name>
```

2. Выполняет соединение указанной ветки с текущей с генерацией коммита слияния
```bash
git merge --no-ff <branch>
```